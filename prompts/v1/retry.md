# Your previous attempt failed validation

Attempt {{attempt}} of {{maxAttempts}} for {{testCaseId}} was rejected at gate {{gate}} ({{failureClass}}).

Problems found:
{{errors}}

{{hints}}

Your previous code:

```ts
{{previousCode}}
```

Fix every problem listed. Keep everything that was already correct. Respond with the complete JSON object again, including the full corrected code.
