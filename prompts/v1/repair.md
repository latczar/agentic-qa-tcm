# Your previous response could not be read

It was not a single valid JSON object matching the required shape. The parser said:

{{errors}}

Respond again with only the JSON object, no prose before or after it, no code fences around it, and the TypeScript file as the value of "code" with newlines escaped.
