# Your previous attempt failed validation

Attempt {{attempt}} of {{maxAttempts}} for {{testCaseId}} was rejected at gate {{gate}} ({{failureClass}}).

Problems found:
{{errors}}

{{hints}}

Your previous code:

```ts
{{previousCode}}
```

Fix every problem listed. Keep everything that was already correct. Respond again with the whole corrected file in one ```ts block, then the ```json metadata block.
